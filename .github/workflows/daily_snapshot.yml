# name: Daily Fandango Scraper

# on:
#   # push:
#   #   branches:
#   #     - master
#   # schedule:
#   #   - cron: '45 * * * *' # Runs at minute 45 every hour
#   workflow_dispatch: # Allows you to manually trigger it via GitHub UI

# permissions:
#   contents: write

# # Concurrency group configured at the workflow level to instantly kill zombie runs
# concurrency:
#   group: fandango-global
#   cancel-in-progress: true

# jobs:
#   scrape-and-upload:
#     runs-on: ubuntu-latest

#     # Hard stop after 20 minutes to prevent the runner from bleeding into the next hour
#     timeout-minutes: 20

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5 # Upgraded to v5 for better stability and caching
#         with:
#           python-version: '3.10'
#           cache: 'pip'

#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Cache Playwright Browsers
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/ms-playwright
#           key: playwright-${{ runner.os }}-${{ hashFiles('requirements.txt') }} # Added hash key so cache invalidates when deps change

#       - name: Install Playwright Browsers
#         run: playwright install chromium

#       - name: Run Fandango Scraper
#         env:
#           FIREBASE_CREDENTIALS: ${{ secrets.FIREBASE_CREDENTIALS }}
#           FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
#           OVERWRITE_SNAPSHOT: "true"
#           # Safeguard: Force headless browsers to gracefully terminate if the runner hits a wall
#           PLAYWRIGHT_BROWSER_TRACKING: "1" 
#         run: |
#           python fandango_noblockv4.py

#       - name: Commit and Push Image Report
#         run: |
#           git config --local user.email "action@github.com"
#           git config --local user.name "GitHub Action"

#           git add latest_report.png || true
#           git add latest_report.xlsx || true
#           git add previous_report.xlsx || true

#           if git diff --staged --quiet; then
#             echo "No changes to commit"
#           else
#             git commit -m "Auto-update latest image report [skip ci]"
#             git push
#           fi