# name: Hourly Fandango Scraper

# on:
#   # push:
#   #   branches:
#   #     - master
#   # schedule:
#   #   - cron: '45 * * * *' # Runs at minute 45 every hour
#   workflow_dispatch: # Allows you to manually trigger it via GitHub UI

# permissions:
#   contents: write

# concurrency:
#   group: fandango-global
#   cancel-in-progress: true

# jobs:
#   scrape-and-upload:
#     runs-on: ubuntu-latest

#     timeout-minutes: 20

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#           cache: 'pip'

#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Cache Playwright Browsers
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/ms-playwright
#           key: playwright-${{ runner.os }}-${{ hashFiles('requirements.txt') }}

#       - name: Install Playwright Browsers
#         run: playwright install chromium

#       - name: Run Fandango Scraper
#         env:
#           FIREBASE_CREDENTIALS: ${{ secrets.FIREBASE_CREDENTIALS }}
#           FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
#           OVERWRITE_SNAPSHOT: "false"
#           PLAYWRIGHT_BROWSER_TRACKING: "1"
#         run: |
#           python fandango_noblockv4.py

#       - name: Commit and Push Image Report
#         run: |
#           git config --local user.email "action@github.com"
#           git config --local user.name "GitHub Action"

#           git add latest_report.png || true
#           git add latest_report.xlsx || true
#           git add previous_report.xlsx || true

#           if git diff --staged --quiet; then
#             echo "No changes to commit"
#           else
#             git commit -m "Auto-update latest image report [skip ci]"
#             git push
#           fi